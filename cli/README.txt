This directory contains the source files to implement the command-line wrapper
around FactoredSegmenter. They are only used in the standalone build (i.e.
FactoredSegmenter as a standalone command-line tool).
